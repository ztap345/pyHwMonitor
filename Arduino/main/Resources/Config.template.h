#define BAUD_RATE ${baud_rate_i}  // int

#define WAIT_TEXT ${wait_text_s}  // string

#define START_SUFFIX ${start_suffix_c} // char
#define READY_CHAR ${ready_char_c}
#define KEY_VAL_SEPARATOR ${key_val_separator_char_c}
#define STOP_CHAR ${stop_char_c}
#define NEWLINE '\n'
#define READY_TIMEOUT ${ready_timeout_secs_i} * ${seconds_to_millis_i}  // millis to seconds
#define WAIT_TIME ${wait_time_secs_i} * ${seconds_to_millis_i}

#define LCD_WAIT_TEXT ${lcd_wait_text_s}
#define LCD_READY_TEXT ${lcd_ready_text_s}
#define LCD_PRINT_DELAY ${lcd_print_delay_i}
#define LCD_SCROLL_DELAY ${lcd_scroll_delay_i}
